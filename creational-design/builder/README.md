# Builder design pattern

### Builder is a creational design pattern, which allows constructing complex objects step by step.
### Handle the construction process of a complex object separately from its representation.
### With large number of attributes, constructors can become cumbersome.
### Builder pattern provides a flexible solution to create such objects.
### Using this we can create immutable objects with many optional parameters.
### Example: StringBuilder class in Java.


